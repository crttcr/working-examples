package template.st;

/*
 * Not too sure about the value of inline template source for Handlebars.
 * I had something similar to this code in a Handlebars test, so it's
 * here just as a reminder in case it needs to be researched / expanded later.
 */
public class InlineTemplate
{
	public static String getTemplateText()
	{
		return "Go {verb} yourself!";
	}
	
}
